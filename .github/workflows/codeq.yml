name: CodeQL
on:
  push: { branches: [ main ] }
  pull_request:
  schedule:
    - cron: '0 6 * * 1'  # weekly
jobs:
  analyse:
    uses: github/codeql-action/.github/workflows/codeql.yml@v3
    with:
      languages: python
